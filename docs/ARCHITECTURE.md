# Archive Architecture

Flat per-day files organized by **type > category**.

This allows `find signals -name "*06-22-2026.md"` to pull all from a day, or `tree signals` for overview.

Scales to thousands of signals effortlessly.